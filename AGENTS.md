# Codex 开发行为规范

## 使用范围

本文件的内容仅用于给 Codex 作为 System Prompt 使用。其他 Agent 不可读取、引用、复用或遵循本文件中的任何内容。

## 语言要求

所有说明性文字必须全部使用中文书写，包括但不限于需求说明、实现说明、验证说明、阶段性总结、代码注释和面向用户的解释。

## 需求流程

进入需求评审、需求实现和需求验证阶段时，默认使用 OpenSpec 工具推进流程。除非用户明确要求使用其他流程，否则应优先通过 OpenSpec 完成需求澄清、方案制定、任务拆解、实现跟踪和验证记录。

## Git 提交

每次完成代码实现或文件修改并通过必要验证后，应自动创建 Git 提交。

提交前必须检查暂存范围，确保只提交本次任务相关文件，不混入用户或其他任务的未提交改动。

提交信息应根据本次更改内容进行总结，简洁说明主要行为变化、文档更新或修复点。

Commit Message 应尽可能使用中文描述；仅当命令名、包名、模块名、惯用提交类型或其他代码标识更适合保留英文时，可以保留必要英文。

## 代码说明与注释

默认将用户视为一个对 Python 语言以及 LangChain、LangGraph 框架完全不了解的人。

编写代码时，应尽可能在关键位置添加中文注释，帮助用户理解：

- 类必须编写中文类注释，说明该类的职责、使用场景以及与其他模块的关系；
- 方法和函数必须编写中文方法注释，说明输入、输出、主要流程和关键副作用；
- 当前代码块的作用；
- 关键变量或对象的含义；
- 函数、类、模块之间的依赖关系；
- LangChain 或 LangGraph 相关概念在当前代码中的用途；
- 非显而易见的流程分支、状态流转和异常处理逻辑。

注释应服务于理解代码逻辑，避免机械重复代码本身已经清楚表达的内容。
